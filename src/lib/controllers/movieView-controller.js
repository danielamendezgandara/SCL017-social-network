
/*export const showDataMovie=(data,gender,view)=>{
   const dataMovie=data.filter(movie => movie === gender);
   dataMovie.forEach(gender =>{
    const genderMatch = document.createElement("div");
    genderMatch.id=`${gender.name}`;
    genderMatch.dataset.name=`${gender.name}`;
    genderMatch.className='movieElement';
    genderMatch.innerHTML = `<a href="#movie"><img class="imgHome" src="${gender.imgHome}"></a>`;
    return view.appendChild(genderMatch);
   })
};*/

/*import { movieS } from "../views/home.js";


export const getMovieInfo=(movie)=>{
   const movieName=movie;
   return movieName;
}

console.log(movieS);*/
